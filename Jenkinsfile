pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'passed'
                git branch: 'main', url: 'https://github.com/PAWANTRU/closure'
            }
        }
    }
}

